var EMOLA;
(function (EMOLA) {
    var EventManager = (function () {
        function EventManager() {
        }
        EventManager.prototype.move = function (color) {
        };
        return EventManager;
    })();
    EMOLA.EventManager = EventManager;
})(EMOLA || (EMOLA = {}));

