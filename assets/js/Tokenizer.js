EMOHUB === undefined ? (throw "Not defined.")

EMOHUB.Parser = function (stringList) {
  this.stringList = stringList;
};

EMOHUB.Tokenizer.prototype = {
  constructor: EMOHUB.Tokenizer
  tokenize : function () {
    this.stringLi



  }
};
