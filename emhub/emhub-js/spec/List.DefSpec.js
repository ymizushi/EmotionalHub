describe("GraphExpList.Def Spec", function () {
  // it("integration", function() {
  //   var defList = new EMOLA.GraphExpList.Def([new EMOLA.Atom(EMOLA.Atom.DEF), new EMOLA.Atom(EMOLA.Atom.VAR, 'hoge'), new EMOLA.Atom(EMOLA.Atom.NUMBER, 2)]);
  //     defList.evalSyntax(EMOLA.Global.env);
  //     expect(EMOLA.Global.env.get('hoge')).toEqual();
  // });
});

